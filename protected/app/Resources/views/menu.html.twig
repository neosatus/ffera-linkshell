{% if user is not defined and is_granted('IS_AUTHENTICATED_ANONYMOUSLY') and not is_granted('ROLE_USER') %}
	<li><a href="{{ path('fos_user_registration_register') }}">Register</a></li>
{% endif %}
{% if is_granted('ROLE_ADMIN') %}
    <li><a href="{{ path('raid_manager') }}">Admin</a></li>
{% endif %}
{% if is_granted('ROLE_USER') %}
    <li><a href="{{ path('home') }}">Home</a></li>
    <li><a href="{{ path('view_info') }}">LS Info</a></li>
    <li><a href="{{ path('view_roster') }}">View Roster</a></li>
    <li><a href="{{ path('edit_mains') }}">Choose Mains</a></li>
    <li><a href="{{ path('fos_user_security_logout') }}">Logout</a></li>
{% endif %}